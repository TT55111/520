# 520 情侣纪念网站实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为伍文韬和程洁搭建一个粉色浪漫风格的情侣纪念网站，包含恋爱计时器、照片墙、爱心动画和背景音乐

**Architecture:** 单页应用，使用 Astro + Tailwind CSS 构建，纯前端本地存储（IndexedDB），无需后端服务。页面分为 Hero（计时器+名字+爱心）、照片墙（上传/预览/删除）、音乐播放器三个区域

**Tech Stack:** Astro 5 + Tailwind CSS + vanilla JavaScript + localforage (IndexedDB 封装)

---

## 文件结构

```
d:\编程\520-memorial/
├── public/
│   ├── favicon.svg              # 网站图标（爱心主题）
│   └── music/
│       └── bg.mp3               # 背景音乐（用户自行放置）
── src/
│   ├── config.ts                # 网站配置（名字、日期、音乐路径）
│   ├── styles/
│   │   └── global.css           # 全局样式（粉色系主题 + 动画定义）
│   ├── components/
│   │   ├── Hero.astro           # Hero 区域：名字 + 计时器 + 爱心动画
│   │   ├── PhotoGallery.astro   # 照片墙组件
│   │   ├── PhotoModal.astro     # 照片预览 Modal
│   │   ├── MusicPlayer.astro    # 音乐播放器组件
│   │   └── FloatingHearts.astro # 漂浮爱心粒子背景
│   ├── pages/
│   │   └── index.astro          # 主页面（组合所有组件）
│   ├── lib/
│   │   └── storage.ts           # IndexedDB 存储封装（localforage）
│   └── layouts/
│       └── BaseLayout.astro     # 基础布局模板
├── astro.config.mjs
├── tailwind.config.mjs
├── tsconfig.json
├── package.json
└── README.md
```

---

### Task 1: 初始化 Astro 项目

**Files:**
- Create: `d:\编程\520-memorial/` 全新项目目录
- Create: `package.json`
- Create: `astro.config.mjs`
- Create: `tailwind.config.mjs`
- Create: `tsconfig.json`

- [ ] **Step 1: 初始化项目**

在 `d:\编程\520-memorial` 目录下执行：

```bash
mkdir d:\编程\520-memorial
cd d:\编程\520-memorial
npm create astro@latest . -- --template minimal --no-install --no-git
```

- [ ] **Step 2: 安装依赖**

```bash
npm install
npm install -D tailwindcss @tailwindcss/vite
npm install localforage
```

- [ ] **Step 3: 配置 Tailwind CSS**

修改 `astro.config.mjs`:

```js
import { defineConfig } from "astro/config";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  vite: {
    plugins: [tailwindcss()],
  },
});
```

创建 `src/styles/global.css`:

```css
@import "tailwindcss";

/* 粉色系主题 */
@theme {
  --color-rose-dark: #d6336c;
  --color-rose-light: #ff6b9d;
  --color-rose-soft: #e06c8a;
  --color-rose-bg: #fff0f3;
  --color-rose-card: #fff5f8;
}

/* 全局样式 */
body {
  font-family: "PingFang SC", "Microsoft YaHei", sans-serif;
  background-color: #fff0f3;
  overflow-x: hidden;
}

/* 心跳动画 */
@keyframes heartbeat {
  0%, 100% { transform: scale(1); }
  15% { transform: scale(1.15); }
  30% { transform: scale(1); }
  45% { transform: scale(1.1); }
}

.animate-heartbeat {
  animation: heartbeat 2s ease-in-out infinite;
}

/* 漂浮动画 */
@keyframes floatUp {
  0% { transform: translateY(100vh) scale(0.3); opacity: 0; }
  10% { opacity: 1; }
  90% { opacity: 0.8; }
  100% { transform: translateY(-10vh) scale(1); opacity: 0; }
}
```

- [ ] **Step 4: 验证项目可运行**

```bash
npm run dev
```

访问 http://localhost:4321 确认首页正常显示。

- [ ] **Step 5: 提交**

```bash
git init
git add .
git commit -m "feat: init 520 memorial project with Astro + Tailwind"
```

---

### Task 2: 创建基础配置和布局

**Files:**
- Create: `src/config.ts`
- Create: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: 创建配置文件**

创建 `src/config.ts`:

```ts
/**
 * 网站配置文件
 * 修改此处可自定义网站内容
 */
export const config = {
  // 恋爱开始日期（格式：YYYY-MM-DD）
  startDate: "2025-09-28",
  // 两人名字
  name1: "伍文韬",
  name2: "程洁",
  // 背景音乐路径（相对于 public 目录）
  musicUrl: "/music/bg.mp3",
  // 页面标题
  pageTitle: "伍文韬 ❤ 程洁 - 我们的爱情故事",
};
```

- [ ] **Step 2: 创建基础布局**

创建 `src/layouts/BaseLayout.astro`:

```astro
---
import "../styles/global.css";

interface Props {
  title?: string;
}

const { title = "伍文韬 ❤ 程洁" } = Astro.props;
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
</head>
<body>
  <slot />
</body>
</html>
```

- [ ] **Step 3: 提交**

```bash
git add src/config.ts src/layouts/BaseLayout.astro
git commit -m "feat: add config and base layout"
```

---

### Task 3: 创建 Hero 组件（计时器 + 名字 + 爱心）

**Files:**
- Create: `src/components/Hero.astro`

- [ ] **Step 1: 实现 Hero 组件**

创建 `src/components/Hero.astro`:

```astro
---
import { config } from "../config";

const { name1, name2, startDate } = config;
---

<section id="hero" class="min-h-screen flex flex-col items-center justify-center relative px-4">
  <!-- 背景装饰 -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <div class="absolute top-1/4 left-1/4 text-rose-light/20 text-6xl animate-heartbeat">💕</div>
    <div class="absolute top-1/3 right-1/4 text-rose-light/15 text-4xl animate-heartbeat" style="animation-delay: 0.5s;">💗</div>
    <div class="absolute bottom-1/4 left-1/3 text-rose-light/15 text-5xl animate-heartbeat" style="animation-delay: 1s;">💖</div>
  </div>

  <!-- 名字 -->
  <div class="text-center z-10 mb-8">
    <h1 class="text-4xl md:text-6xl font-bold text-rose-dark mb-4">
      {name1} <span class="animate-heartbeat inline-block">❤</span> {name2}
    </h1>
    <p class="text-rose-soft text-lg">我们的爱情故事</p>
  </div>

  <!-- 计时器 -->
  <div class="bg-white/80 backdrop-blur-sm rounded-3xl p-8 md:p-12 shadow-lg z-10 text-center max-w-lg w-full">
    <p class="text-rose-soft text-sm mb-4">我们已经在一起</p>
    <div id="timer" class="text-3xl md:text-5xl font-bold text-rose-dark font-mono">
      计算中...
    </div>
    <p class="text-rose-soft text-sm mt-4">
      从 {new Date(startDate).toLocaleDateString("zh-CN")} 开始
    </p>
  </div>

  <!-- 向下箭头 -->
  <div class="absolute bottom-8 animate-bounce text-rose-soft">
    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>

<script is:inline>
  // 恋爱计时器 - 每秒更新
  const startDate = new Date("{startDate}T00:00:00").getTime();
  
  function updateTimer() {
    const now = Date.now();
    const diff = now - startDate;
    
    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((diff % (1000 * 60)) / 1000);
    
    const timerEl = document.getElementById("timer");
    if (timerEl) {
      timerEl.textContent = `${days} 天 ${hours} 时 ${minutes} 分 ${seconds} 秒`;
    }
  }
  
  updateTimer();
  setInterval(updateTimer, 1000);
</script>
```

- [ ] **Step 2: 验证**

启动开发服务器，访问页面确认计时器正常显示并每秒更新。

- [ ] **Step 3: 提交**

```bash
git add src/components/Hero.astro
git commit -m "feat: add hero section with countdown timer"
```

---

### Task 4: 创建漂浮爱心粒子背景

**Files:**
- Create: `src/components/FloatingHearts.astro`

- [ ] **Step 1: 实现漂浮爱心组件**

创建 `src/components/FloatingHearts.astro`:

```astro
---
// 漂浮爱心粒子背景组件
// 使用 CSS 动画创建随机漂浮的爱心动画效果
---

<div id="floating-hearts" class="fixed inset-0 pointer-events-none z-0 overflow-hidden" aria-hidden="true">
</div>

<script is:inline>
  (function () {
    const container = document.getElementById("floating-hearts");
    if (!container) return;
    
    const hearts = ["💕", "💗", "💖", "💓", "❤️"];
    const particleCount = 20; // 控制粒子数量，避免性能问题
    
    function createHeart() {
      const heart = document.createElement("div");
      heart.textContent = hearts[Math.floor(Math.random() * hearts.length)];
      heart.style.cssText = `
        position: absolute;
        left: ${Math.random() * 100}%;
        font-size: ${Math.random() * 20 + 12}px;
        opacity: 0;
        animation: floatUp ${Math.random() * 8 + 8}s linear infinite;
        animation-delay: ${Math.random() * 10}s;
      `;
      container.appendChild(heart);
    }
    
    // 初始创建粒子
    for (let i = 0; i < particleCount; i++) {
      createHeart();
    }
  })();
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/components/FloatingHearts.astro
git commit -m "feat: add floating hearts particle background"
```

---

### Task 5: 创建存储层（IndexedDB 封装）

**Files:**
- Create: `src/lib/storage.ts`

- [ ] **Step 1: 实现存储封装**

创建 `src/lib/storage.ts`:

```ts
/**
 * 照片存储封装 - 使用 localforage (IndexedDB 封装库)
 * 提供照片的增删查功能
 */
import localforage from "localforage";

// 初始化存储实例
const photoStore = localforage.createInstance({
  name: "love520",
  storeName: "photos",
});

/**
 * 照片数据结构
 */
export interface PhotoItem {
  id: string;        // 唯一 ID
  dataUrl: string;   // Base64 图片数据
  name: string;      // 文件名
  uploadedAt: number; // 上传时间戳
}

/**
 * 保存照片到 IndexedDB
 * @param photo - 照片对象
 */
export async function savePhoto(photo: PhotoItem): Promise<void> {
  await photoStore.setItem(photo.id, photo);
}

/**
 * 获取所有照片（按上传时间倒序）
 */
export async function getAllPhotos(): Promise<PhotoItem[]> {
  const photos: PhotoItem[] = [];
  await photoStore.iterate((value: PhotoItem) => {
    photos.push(value);
  });
  // 按上传时间倒序排列
  return photos.sort((a, b) => b.uploadedAt - a.uploadedAt);
}

/**
 * 删除照片
 * @param id - 照片 ID
 */
export async function deletePhoto(id: string): Promise<void> {
  await photoStore.removeItem(id);
}

/**
 * 生成唯一 ID
 */
export function generateId(): string {
  return Date.now().toString(36) + Math.random().toString(36).slice(2);
}
```

- [ ] **Step 2: 安装类型声明**

```bash
npm install --save-dev @types/localforage 2>nul || echo "localforage already has types"
```

- [ ] **Step 3: 提交**

```bash
git add src/lib/storage.ts
git commit -m "feat: add IndexedDB photo storage wrapper"
```

---

### Task 6: 创建照片墙组件

**Files:**
- Create: `src/components/PhotoGallery.astro`

- [ ] **Step 1: 实现照片墙组件**

创建 `src/components/PhotoGallery.astro`:

```astro
---
/**
 * 照片墙组件
 * 功能：拖拽上传、网格展示、点击预览、右键删除
 */
---

<section id="gallery" class="py-16 px-4 max-w-6xl mx-auto relative z-10">
  <h2 class="text-3xl font-bold text-rose-dark text-center mb-8">我们的回忆 💕</h2>
  
  <!-- 拖拽上传区域 -->
  <div 
    id="drop-zone"
    class="border-2 border-dashed border-rose-light/50 rounded-2xl p-8 text-center mb-8 cursor-pointer hover:bg-rose-card/50 transition-colors"
  >
    <div class="text-rose-soft">
      <svg class="w-12 h-12 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
      </svg>
      <p class="text-lg font-medium">拖拽照片到这里</p>
      <p class="text-sm mt-1">或点击选择照片</p>
    </div>
    <input type="file" id="file-input" class="hidden" accept="image/*" multiple />
  </div>

  <!-- 照片网格 -->
  <div id="photo-grid" class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    <!-- 照片将通过 JavaScript 动态渲染 -->
  </div>

  <!-- 空状态 -->
  <div id="empty-state" class="text-center py-12 text-rose-soft">
    <p class="text-lg">还没有照片哦~ 快上传你们的甜蜜瞬间吧 💕</p>
  </div>
</section>

<script is:inline>
  import { savePhoto, getAllPhotos, deletePhoto, generateId } from "../lib/storage";
</script>

<script>
  import { savePhoto, getAllPhotos, deletePhoto, generateId, type PhotoItem } from "../lib/storage";

  const dropZone = document.getElementById("drop-zone");
  const fileInput = document.getElementById("file-input") as HTMLInputElement;
  const photoGrid = document.getElementById("photo-grid");
  const emptyState = document.getElementById("empty-state");

  // 压缩图片（限制最大尺寸）
  function compressImage(file: File): Promise<string> {
    return new Promise((resolve) => {
      const reader = new FileReader();
      reader.onload = (e) => {
        const img = new Image();
        img.onload = () => {
          const canvas = document.createElement("canvas");
          const maxSize = 800;
          let width = img.width;
          let height = img.height;
          
          if (width > height && width > maxSize) {
            height = (maxSize / width) * height;
            width = maxSize;
          } else if (height > maxSize) {
            width = (maxSize / height) * width;
            height = maxSize;
          }
          
          canvas.width = width;
          canvas.height = height;
          const ctx = canvas.getContext("2d")!;
          ctx.drawImage(img, 0, 0, width, height);
          resolve(canvas.toDataURL("image/jpeg", 0.8));
        };
        img.src = e.target!.result as string;
      };
      reader.readAsDataURL(file);
    });
  }

  // 渲染照片网格
  function renderPhotos(photos: PhotoItem[]) {
    if (photos.length === 0) {
      emptyState!.style.display = "block";
      photoGrid!.innerHTML = "";
      return;
    }
    
    emptyState!.style.display = "none";
    photoGrid!.innerHTML = photos.map((photo) => `
      <div class="photo-item relative group aspect-square rounded-xl overflow-hidden shadow-md cursor-pointer" 
           data-id="${photo.id}"
           onclick="window.previewPhoto('${photo.id}', '${photo.dataUrl}')"
           oncontextmenu="window.confirmDeletePhoto(event, '${photo.id}')">
        <img src="${photo.dataUrl}" alt="${photo.name}" class="w-full h-full object-cover" loading="lazy" />
        <div class="absolute inset-0 bg-black/0 group-hover:bg-black/30 transition-colors flex items-center justify-center">
          <span class="text-white opacity-0 group-hover:opacity-100 transition-opacity">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0zM10 7v3m0 0v3m0-3h3m-3 0H7" />
            </svg>
          </span>
        </div>
      </div>
    `).join("");
  }

  // 加载并渲染照片
  async function loadPhotos() {
    const photos = await getAllPhotos();
    renderPhotos(photos);
  }

  // 处理文件上传
  async function handleFiles(files: FileList | File[]) {
    for (const file of Array.from(files)) {
      if (!file.type.startsWith("image/")) continue;
      
      const dataUrl = await compressImage(file);
      const photo: PhotoItem = {
        id: generateId(),
        dataUrl,
        name: file.name,
        uploadedAt: Date.now(),
      };
      
      await savePhoto(photo);
    }
    
    await loadPhotos();
  }

  // 拖拽事件
  dropZone!.addEventListener("dragover", (e) => {
    e.preventDefault();
    dropZone!.classList.add("bg-rose-card", "border-rose-dark");
  });

  dropZone!.addEventListener("dragleave", () => {
    dropZone!.classList.remove("bg-rose-card", "border-rose-dark");
  });

  dropZone!.addEventListener("drop", async (e) => {
    e.preventDefault();
    dropZone!.classList.remove("bg-rose-card", "border-rose-dark");
    await handleFiles(e.dataTransfer!.files);
  });

  // 点击上传
  dropZone!.addEventListener("click", () => {
    fileInput!.click();
  });

  fileInput!.addEventListener("change", async (e) => {
    const target = e.target as HTMLInputElement;
    if (target.files) {
      await handleFiles(target.files);
      target.value = "";
    }
  });

  // 预览照片（全局函数）
  (window as any).previewPhoto = (id: string, dataUrl: string) => {
    // 触发自定义事件，由 PhotoModal 组件监听
    document.dispatchEvent(new CustomEvent("photo-preview", { detail: { id, dataUrl } }));
  };

  // 确认删除（全局函数）
  (window as any).confirmDeletePhoto = async (e: MouseEvent, id: string) => {
    e.preventDefault();
    if (confirm("确定要删除这张照片吗？")) {
      await deletePhoto(id);
      await loadPhotos();
    }
  };

  // 初始加载
  loadPhotos();
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/components/PhotoGallery.astro
git commit -m "feat: add photo gallery with drag & drop upload"
```

---

### Task 7: 创建照片预览 Modal

**Files:**
- Create: `src/components/PhotoModal.astro`

- [ ] **Step 1: 实现 Modal 组件**

创建 `src/components/PhotoModal.astro`:

```astro
---
/**
 * 照片预览 Modal
 * 点击照片后放大展示，支持 ESC 关闭、点击外部关闭
 */
---

<div 
  id="photo-modal" 
  class="fixed inset-0 z-50 hidden items-center justify-center bg-black/80 backdrop-blur-sm"
  onclick="window.closePhotoModal(event)"
>
  <div class="relative max-w-4xl max-h-[90vh] p-4" onclick="event.stopPropagation()">
    <button 
      onclick="window.closePhotoModal()"
      class="absolute -top-2 -right-2 text-white hover:text-rose-light transition-colors z-10"
    >
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
    <img 
      id="modal-image" 
      src="" 
      alt="照片预览" 
      class="max-w-full max-h-[85vh] object-contain rounded-lg shadow-2xl" 
    />
  </div>
</div>

<script is:inline>
  // 监听照片预览事件
  document.addEventListener("photo-preview", ((e: CustomEvent) => {
    const { dataUrl } = e.detail;
    const modal = document.getElementById("photo-modal");
    const modalImage = document.getElementById("modal-image") as HTMLImageElement;
    
    if (modal && modalImage) {
      modalImage.src = dataUrl;
      modal.classList.remove("hidden");
      modal.classList.add("flex");
    }
  }) as EventListener);

  // 关闭 Modal（全局函数）
  (window as any).closePhotoModal = (event?: MouseEvent) => {
    // 如果点击的是背景遮罩层，关闭 Modal
    if (event && event.target !== event.currentTarget) return;
    
    const modal = document.getElementById("photo-modal");
    if (modal) {
      modal.classList.add("hidden");
      modal.classList.remove("flex");
    }
  };

  // ESC 键关闭
  document.addEventListener("keydown", ((e: KeyboardEvent) => {
    if (e.key === "Escape") {
      (window as any).closePhotoModal();
    }
  }) as EventListener);
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/components/PhotoModal.astro
git commit -m "feat: add photo preview modal with ESC close"
```

---

### Task 8: 创建音乐播放器组件

**Files:**
- Create: `src/components/MusicPlayer.astro`

- [ ] **Step 1: 实现音乐播放器**

创建 `src/components/MusicPlayer.astro`:

```astro
---
import { config } from "../config";

const { musicUrl } = config;
---

<div id="music-player" class="fixed bottom-4 right-4 z-40">
  <!-- 收起状态的按钮 -->
  <button 
    id="music-toggle"
    onclick="window.toggleMusicPlayer()"
    class="bg-white/90 backdrop-blur-sm rounded-full p-3 shadow-lg text-rose-dark hover:bg-rose-card transition-colors"
  >
    <svg id="music-icon-pause" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 9v6m4-6v6m7-3a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>
    <svg id="music-icon-play" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19V6l12-3v13M9 19c0 1.105-1.343 2-3 2s-3-.895-3-2 1.343-2 3-2 3 .895 3 2zm12-3c0 1.105-1.343 2-3 2s-3-.895-3-2 1.343-2 3-2 3 .895 3 2zM9 10l12-3" />
    </svg>
  </button>

  <!-- 隐藏的 audio 元素 -->
  <audio id="bg-music" loop preload="auto">
    <source src="{musicUrl}" type="audio/mpeg" />
  </audio>
</div>

<script is:inline>
  const audio = document.getElementById("bg-music") as HTMLAudioElement;
  const playIcon = document.getElementById("music-icon-play");
  const pauseIcon = document.getElementById("music-icon-pause");
  let isPlaying = false;

  (window as any).toggleMusicPlayer = () => {
    if (isPlaying) {
      audio.pause();
      playIcon!.classList.remove("hidden");
      pauseIcon!.classList.add("hidden");
    } else {
      audio.play().catch(() => {
        // 浏览器可能阻止自动播放，提示用户
        alert("请点击页面任意位置后再尝试播放音乐");
      });
      playIcon!.classList.add("hidden");
      pauseIcon!.classList.remove("hidden");
    }
    isPlaying = !isPlaying;
  };

  // 首次点击页面时尝试播放
  document.addEventListener("click", () => {
    if (!isPlaying) {
      audio.play().then(() => {
        isPlaying = true;
        playIcon!.classList.add("hidden");
        pauseIcon!.classList.remove("hidden");
      }).catch(() => {
        // 静默失败
      });
    }
  }, { once: true });
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/components/MusicPlayer.astro
git commit -m "feat: add music player with play/pause toggle"
```

---

### Task 9: 整合主页面

**Files:**
- Create: `src/pages/index.astro`
- Create: `public/favicon.svg`

- [ ] **Step 1: 创建主页面**

创建 `src/pages/index.astro`:

```astro
---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import FloatingHearts from "../components/FloatingHearts.astro";
import PhotoGallery from "../components/PhotoGallery.astro";
import PhotoModal from "../components/PhotoModal.astro";
import MusicPlayer from "../components/MusicPlayer.astro";
import { config } from "../config";
---

<BaseLayout title={config.pageTitle}>
  <!-- 漂浮爱心背景 -->
  <FloatingHearts />
  
  <!-- Hero 区域 -->
  <Hero />
  
  <!-- 照片墙区域 -->
  <PhotoGallery />
  
  <!-- 照片预览 Modal -->
  <PhotoModal />
  
  <!-- 音乐播放器 -->
  <MusicPlayer />
</BaseLayout>
```

- [ ] **Step 2: 创建 favicon**

创建 `public/favicon.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#ff6b9d;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#d6336c;stop-opacity:1" />
    </linearGradient>
  </defs>
  <path d="M50 88 C25 65 5 50 5 30 C5 15 15 5 30 5 C40 5 47 12 50 18 C53 12 60 5 70 5 C85 5 95 15 95 30 C95 50 75 65 50 88Z" fill="url(#grad)"/>
</svg>
```

- [ ] **Step 3: 验证**

```bash
npm run dev
```

访问 http://localhost:4321 确认所有组件正常展示。

- [ ] **Step 4: 提交**

```bash
git add src/pages/index.astro public/favicon.svg
git commit -m "feat: integrate all components into main page"
```

---

### Task 10: 前端视觉设计与优化

**Files:**
- Modify: `src/styles/global.css`
- Modify: `src/components/Hero.astro`
- Modify: `src/components/PhotoGallery.astro`
- Modify: `src/components/MusicPlayer.astro`

> 此阶段使用 `frontend-design` + `impeccable` skill 进行视觉调优

- [ ] **Step 1: 使用 frontend-design 确定整体视觉风格**
  - 调用 frontend-design skill
  - 确认粉色系浪漫风格的一致性
  - 调整 Hero 区域的视觉层次

- [ ] **Step 2: 使用 impeccable 进行细节打磨**
  - UX 评审：检查交互流畅性
  - 视觉层次：确保信息层级清晰
  - 响应式适配：手机端测试
  - 动效优化：确保爱心动画流畅不卡顿
  - 可访问性：检查对比度、键盘导航

- [ ] **Step 3: 浏览器验证**
  - 使用 webapp-testing skill 在真实浏览器中验证
  - 计时器实时更新
  - 拖拽上传功能
  - 照片预览 Modal
  - 音乐播放器控制

- [ ] **Step 4: 提交**

```bash
git add .
git commit -m "style: polish visual design and UX details"
```

---

## 计划自检

### 1. 规范覆盖检查

| 设计文档要求 | 对应任务 |
|-------------|---------|
| 恋爱计时器（精确到秒） | Task 3 |
| 照片上传（拖拽） | Task 6 |
| 照片网格展示 | Task 6 |
| 照片放大预览 | Task 7 |
| 照片删除 | Task 6 |
| 背景音乐 | Task 8 |
| 动态爱心效果 | Task 4 |
| 粉色浪漫风格 | Task 1, 10 |
| 响应式适配 | Task 10 |
| 配置项（名字/日期） | Task 2 |

### 2. 占位符检查
- 无 TBD/TODO 占位符
- 所有代码步骤都包含完整代码
- 所有文件路径都是具体的

### 3. 类型一致性检查
- `PhotoItem` 接口在 Task 5 定义，在 Task 6 中使用
- 全局函数 `previewPhoto`、`confirmDeletePhoto`、`closePhotoModal` 在各组件间一致
- 配置项 `config` 在 Task 2 定义，在 Task 3、8、9 中使用

---

## 执行方式选择

计划已保存至 `docs/superpowers/plans/2026-05-20-520-memorial.md`。

两种执行方式：

1. **Subagent-Driven（推荐）** - 每个任务分配一个独立 subagent 执行，任务间进行两阶段审查（功能自检 + 集成审查），迭代快
2. **Inline Execution** - 在当前会话中批量执行任务，设置检查点

请选择执行方式。
